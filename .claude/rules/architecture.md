# 設計・アーキテクチャのベストプラクティス

## モジュール設計

- 単一責任の原則（SRP）を守る
- 循環参照を避ける
- インターフェースに依存（依存性逆転）

## エラーハンドリング

- カスタム例外クラスを定義する
- 例外は具体的に catch する（bare except 禁止）
- ログにはコンテキスト情報を含める

## 命名規則

- 関数名は動詞で始める（`get_`, `create_`, `update_`）
- クラス名は名詞（`UserProfile`, `OrderManager`）
- 定数は UPPER_SNAKE_CASE
